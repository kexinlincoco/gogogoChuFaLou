# 出发喽——AI对话式酒店预订App

## 产品需求文档 (PRD)

| | |
|---|---|
| 文档状态 | 已有可运行的全栈实现（`backend/` + `frontend/`），随开发/试用反馈持续更新；本文只反映当前最新状态，不逐版本记录历史变更 |
| 适用场景 | 课程项目/产品提案（CIS 7000-008） |
| 编写日期 | 2026-09-02 初稿，随后持续修订 |
| 目标平台 | 移动端 App（iOS + Android）——MVP阶段以响应式 Web App 落地（手机/桌面浏览器均可访问），而非原生App，见7.6 |

---

## 一、背景与问题

传统订酒店类App（携程、飞猪、Booking等）的核心交互逻辑是"筛选器驱动"：用户需要手动输入目的地、日期，再逐个调整价格区间、星级、设施等筛选条件，才能从大量结果中找到符合自己偏好的酒店。这个过程存在两个明显痛点：

1. **决策成本高**：可选项太多，用户需要反复试错式地调整筛选条件，耗时长，容易产生"选择疲劳"。
2. **筛选条件无法表达真实诉求**：用户真正在意的往往是"安静""干净""性价比高""适合看夜景"这类主观、模糊的偏好，而传统筛选器只能处理结构化的硬性条件（价格区间、星级），无法承载这些真实诉求。

与此同时，用户在旅行决策场景中已经养成了一种新的行为习惯：在小红书这类内容平台上"提问"，通过真实用户的评论和回答来获取参考，而不是单纯依赖平台的结构化筛选和排序。这说明"真实评论"本身是一种被验证过的、用户信任的决策依据，但目前主流订房App并未把评论数据深度整合进AI推荐的解释逻辑中。

基于以上两点，我们希望做一款以**AI对话**为核心交互方式的酒店预订App"出发喽"：用户像和朋友聊天一样说出诉求（去哪里、预算多少、想要什么样的体验），AI基于真实用户评论数据给出有理由、可解释的推荐，同时保留传统手动筛选模式供不习惯聊天交互的用户选择。

---

## 二、市场与竞品调研

在正式定义产品目标之前，我们对国内外主要OTA/住宿平台的AI能力现状做了调研，用于判断"AI聊天订酒店"这件事的差异化空间在哪里。

### 国内

- **携程"问道"**（2023年发布，行业首个旅游垂直大模型）：起步最早，但第三方实测反馈问题较多——上下文理解弱（把"两人出行"误解为"三人"）、多天行程规划不完整（要求排5天行程只排出3天）、容易在用户追问时重复给出相同答案，被评价为本质上是"网络素材整合工具"而非真正具备理解和执行能力的助手。不过携程聊天机器人TripGenie的流量增长了200%，说明"聊天找酒店/找行程"这个交互形态本身有真实的用户需求，只是携程的执行质量被诟病。（[woshipm评测](https://www.woshipm.com/it/6125667.html)）
- **飞猪"问一问/飞猪帮帮"**（2025年4月发布）：支持一句话表达复合条件（例如"1000元以下、能看到东方明珠夜景的酒店"），具备"主动打电话给酒店争取免费升房"这类代理式动作，并针对差旅、自驾、家庭出游分化出专属助手，强调"跨时空记忆"用户历史偏好。局限是交通、饮食信息覆盖不足，目前仍在"每周迭代"的实验阶段。（[163新闻](https://www.163.com/dy/article/L44K530J05129QAF.html)）
- **同程旅行 × DeepSeek "DeepTrip"**（2025年3月）：第三方对比评测中表现最突出，创新点是会先反问用户基础但常被忽略的信息（如"从哪里出发"），随后给出具体车次/航班/酒店并直接链接预订，细到演出时间和穿搭建议，据称接入AI后员工人效提升43%。（[woshipm对比测评](https://www.woshipm.com/it/6202035.html)）
- **美团"既白"**：定位是面向酒店商家的经营端AI工具，而非面向消费者的聊天订房助手，说明美团在C端对话式订房上暂时是空白。

### 海外

- **Expedia "Romie"**：强调"hyper-personalization"而非传统筛选，能读取用户邮件、监听家庭群聊里的行程讨论，主动应对天气变化和行程变更，定位更接近"AI礼宾"。（[HotelDive](https://www.hoteldive.com/news/expedia-ai-assistant-romie/716315/)）
- **Airbnb**：2026年测试用自然语言搜索取代传统筛选，CEO明确提出要让AI"不只是帮你搜索，而是懂你"。（[TechCrunch](https://techcrunch.com/2026/02/13/airbnb-plans-to-bake-in-ai-features-for-search-discovery-and-support/)）

### 调研结论

"用聊天代替筛选"这件事本身，在2025-2026年已经是各大平台的标配能力，**不构成独立的差异化壁垒**。真正存在差异化空间、也是各家现存短板集中的地方是：

1. 对话的**上下文理解和执行准确度**（携程问道的短板）；
2. 推荐是否有**可信、可解释的依据**，而不只是参数匹配（目前几家都没有把真实评论深度整合进推荐解释逻辑）；
3. 是否真正做到"越用越懂你"的长期个性化（Airbnb、Expedia正在探索，但尚处早期）。

因此本产品选择以**真实评论驱动的可解释推荐**作为MVP阶段的核心差异化定位，而非单纯复刻"聊天筛选"。

---

## 三、产品目标与非目标

### 目标（MVP阶段）

1. 让用户能够用自然语言聊天的方式表达订酒店诉求，AI基于真实用户评论给出有理由的酒店推荐。
2. 保留传统手动筛选模式，两种模式可自由切换，服务不同使用习惯的用户。
3. 验证"AI主动追问 + 反哺评论区"这一数据飞轮机制在真实交互中的可行性和体验效果。
4. 完成从推荐到预订、支付的完整闭环体验（App内直接完成，不跳转第三方）。

### 非目标（本阶段不做，列入未来路线图）

- 不做基于用户历史行为的深度个性化推荐引擎（如自动学习用户长期偏好并免输入直接推荐）；本期AI的"懂你"仅限于单次对话内的上下文记忆，以及对已声明偏好的重复利用。
- 不做多人协作订房、行程规划、交通/景点推荐等泛旅行场景功能。
- 不对接真实酒店库存/支付网关等商业合作，MVP阶段数据为调研假设下的模拟数据（见第八章）。
- 不做面向酒店商家端的经营工具。

---

## 四、目标用户

### 核心人群（MVP优先服务）：休闲/自由行游客

个人或家庭出游为主，决策关注点是性价比、目的地体验、房型是否适合同行人数；对价格敏感，愿意花时间"聊"清楚需求以换取更精准的推荐，但不希望花时间在反复调整筛选条件上。

> 说明：产品未来会考虑覆盖商务差旅人群（关注效率、位置、报销合规），但两类用户对AI追问的内容和推荐排序逻辑差异较大，MVP阶段优先把休闲游客场景做扎实，商务场景列入路线图。

### 用户画像示例

**用户：小林，27岁，互联网行业，和男友两人出行**
- 场景：周末想临时决定去哪玩，没有明确目的地，只有模糊的"想放松、看海"的诉求
- 痛点：不知道该输入哪个城市，也不确定该选什么价位/星级，打开传统App容易"逛了半小时还没下单"
- 期待：像问朋友一样说出诉求，得到几个"靠谱"的推荐，并知道"为什么推荐这家"

---

## 五、核心用户场景 / 用户故事

1. 作为一个用户，我希望一打开App就能直接说出我的诉求（去哪里、预算多少、想要什么样的体验），而不是先面对一堆筛选条件。
2. 作为一个用户，我希望AI推荐酒店时能告诉我"为什么推荐这家"，并且这个理由是基于真实住客的评论，而不是单纯的销量或星级排序。
3. 作为一个用户，我希望能直接用口语化的方式追加或修改条件（比如"这家有点贵，便宜一点的"、"我不想要靠马路的，太吵了"），AI能听懂并调整推荐。
4. 作为一个用户，我更习惯手动筛选，我希望能一键切换到传统的筛选+列表模式，而不是被强制使用聊天。
5. 作为一个用户，如果我住过某家酒店但没写评论，我希望AI能在合适的时机随口问我"住得怎么样"，而不是让我去找入口填写正式评价表单。
6. 作为一个产品运营方，我希望用户在聊天中随口给出的反馈能够被结构化地沉淀下来，反哺到该酒店的评论区，帮助后来的用户做决策，同时补充平台对该酒店数据不足的问题。

---

## 六、产品功能需求（MVP）

### 6.1 AI对话式订酒店（核心功能）

- **入口**：App打开后默认进入AI聊天界面（对话优先的信息架构），而非传统的搜索/筛选首页。AI以轻松、朋友式的语气主动开启话题（例如："嗨～这次想去哪儿玩呀？跟我说说你的想法，我来帮你挑酒店～"）。
- **称呼与账号绑定**：浏览和聊天全程不要求登录，AI在没有称呼可用时用不带名字的通用问候语（"嗨～这次想去哪儿玩呀？..."）。称呼本身不单独采集，而是和6.5节的手机号登录绑在一起：用手机号**第一次**登录（预订时触发）时，登录框会多问一句"怎么称呼你？"，填的名字就锁定成这个手机号账号的名字；同一个手机号**以后**登录，直接沿用锁定的名字，不会重新问。
- **信息采集**：AI需要通过对话获取以下信息才能给出推荐：
  - 必填（缺失时AI必须主动追问）：目的地、入住/离店日期、预算区间。
  - 选填（有合理默认值，不强制打断对话）：入住人数/房间数（默认2人1间）、星级/位置偏好、软性偏好（安静、海景、适合亲子、氛围感等）。
- **多轮修正**：用户可在推荐结果出来后继续用自然语言追加/修改条件（如"便宜一点""不要靠马路"），AI在同一对话上下文中重新调整推荐，不要求用户重新完整描述一遍需求。
- **上下文记忆边界（MVP假设）**：AI需要在单次对话会话内保持上下文（不能出现"聊着聊着就忘了之前说的城市"这类问题——这是竞品携程问道被明确诟病的短板，本产品应在验收标准中重点测试这一点）。跨会话的长期偏好记忆列入未来路线图。
- **数据覆盖范围的诚实性**：MVP阶段酒店数据只覆盖有限的几个城市（见第八章）。当用户想去的城市不在覆盖范围内时，AI**不能静默地**换成其他城市的酒店冒充结果，正确行为：明确告知用户"目前还没有这个城市的数据"，可以展示其他城市作为参考，但必须清楚说明这是替代方案而不是用户想要的地点。

### 6.2 真实评论驱动的可解释推荐

- 酒店推荐结果中，每个推荐卡片需附带一句基于真实评论生成的"推荐理由摘要"，而非仅展示星级/价格/销量，例如："最近的住客评论里，60%提到隔音效果好、适合休息"。
- 推荐理由的生成方式：以用户对话中提取出的偏好关键词（如"安静""干净""海景"）为查询条件，在该酒店的历史评论文本中检索相关片段，由AI对检索到的真实评论内容做摘要和归纳，而非无依据地自由生成（避免AI幻觉，理由必须可追溯到具体评论）。
- 用户可以点击推荐理由，查看其依据的原始评论片段，保证可信度和透明度。
- **评论优先展示**：从AI推荐卡片点进酒店详情页时，若已知用户在对话中表达的诉求（偏好关键词），详情页的评论列表会把和这些关键词相关的真实评论排到前面，并在列表上方提示"已优先显示和'XX'相关的评论"；从手动筛选模式进入详情页（没有对话上下文）则不做优先排序，只展示下面的高频提到角标。
- **住客高频提到角标**：不论是否有对话上下文，酒店详情页都会展示一条基于该酒店全部真实评论统计出的角标——"住客高频提到：X（Y%的评论，共M/N条提到）"，X是该酒店评论中出现频率最高的话题标签，用于在没有针对性诉求时也能一眼看到这家酒店最鲜明的真实特点。

### 6.3 AI主动追问反馈，反哺评论区（数据飞轮）

这个飞轮实际上分两个时机点问两件不同的事，不是同一次追问里问完的：

- **①AI推荐满意度——下单成功的瞬间就问，不等入住**：预订成功页面上会立刻附带一个"这次AI推荐的酒店，你满意吗？"（`orders.source='ai_chat'`的单）或"这次预订的选择，你满意吗？"（手动筛选下的单，措辞不提"AI推荐"，因为压根不涉及这件事）快捷选项（满意/一般/不满意/先跳过）——**两种来源的订单都问**，不是只有AI推荐的单才问，只是问法根据来源略有不同。选在这个时间点问是因为这本质上是"对这次预订选择质量的反馈"，用户刚做完选择、记忆最新鲜，不需要也不应该等到入住之后才问——等到入住之后问，得到的其实是"对酒店本身的满意度"，会和选择/推荐质量本身混在一起，测不准。
- **②入住体验——真正等到入住日期过去之后才问**：触发条件是用户存在一条状态为已完成、且**入住日期已经过去**（`checkout <= 今天`）、对应的酒店用户尚未提交正式评论的历史订单——这样才是真正意义上的"问一问已经住完的体验"。AI在用户下次打开聊天时，以自然对话的方式提出这一个问题（不是弹出正式评价表单），提问文字和快捷选项、可选详细反馈都在**同一张卡片**里，不是提问单独一条消息、选项另起一块——例如"对了～你之前入住的『XX酒店』，住下来之后感觉干净吗？"配上"很干净/一般/不太干净/先跳过"。**同一用户有多笔都符合条件的历史订单时，优先追问最近退房的那一次**（而不是最早下单的那一次）——离得越近，用户对入住体验记得越清楚、答得越准，这条反馈对未来顾客的参考价值也更高；答完（或跳过）一次会话后，下次才会轮到次近的一笔，一次只问一个，不会同时问多个。
- 两处都支持点几下就完事的结构化快捷选项，选完之后都提供一个**可选**的"详细反馈"入口——文字、照片、语音三选多不互斥，不填也能直接提交：照顾不想多说的低反馈意愿用户，也照顾愿意多说细节的用户。
- **是否公开展示的选择权在用户手上**：填完详细反馈、点提交之前，界面上有一个默认勾选的"同意展示在评论区给其他旅客参考"选项，用户可以取消勾选。同意（默认）：这条反馈会被存入该酒店的评论数据池，展示给其他用户时标注为**"来自AI助手采集的用户反馈"**标签，与用户主动撰写的正式评论做视觉和来源区分，避免真实性混淆。不同意：这条反馈依然会被完整记录下来（用于内部指标、后续优化推荐），只是不会出现在酒店的公开评论列表里——"记录"和"公开展示"是两件事，用户的反馈不会因为不公开就被丢弃。①②两问现在都遵循这套同意机制，不再有"只有②计入评论区、①完全不计入"的区别对待。满意度反馈默认归入"AI推荐"话题标签，不计入入住体验相关的"住客高频提到"统计，避免和干净/位置好等真实房态属性混在一起。语音附件目前只存储、不做转写，因此不会进入评论区的关键词检索或"住客高频提到"统计（见第十二章局限）。
- 该功能同时用于补充平台对"评论数据稀疏"酒店的信息覆盖。
- **"先跳过"不是永久性的，措辞也不该像在追着用户要反馈**：不管是①还是②，点"先跳过"只表示"这一次不想答"，不会把订单标记为"已经问过、永远不再问"——②的入住体验问题如果被跳过，这条订单仍然保持"待追问"状态，下次用户重新打开一个新的对话/登录会话时，还会有机会再被问一次，直到用户真正给出一个实际答案（而非跳过）才会停止追问。这是为了不把"这次不想说"误解成"以后都不会想说"。但AI回应跳过时的措辞也很重要：不能说"没关系，下次再问你～"这类暗示"我还会继续追着你问"的话，会让本来就不想答的用户更反感；改用不带追问压力、鼓励用户"想说的时候随时可以说"的措辞（如"好的，不打扰你啦～之后什么时候想聊聊住的感受，随时都欢迎告诉我～"）。
- **审核/质量控制规则（MVP假设，回应第十二章开放问题1）**：因快捷选项（如"很干净/一般/不太干净"）走无需审核的规则兜底路径，可选的文字/照片/语音详细反馈目前也未引入人工审核队列或NLP内容审核——MVP阶段规则兜底：①同一用户对同一酒店的AI采集反馈最多计入一次，防止重复刷分；②单个酒店展示的"来自AI助手采集"类反馈占该酒店总评论条数的比例设上限（假设30%），避免稀释正式评论的权重；③"先跳过"不生成任何评论区内容。更完善的可信度加权机制、详细反馈的审核机制列入未来路线图（见十一章）。

### 6.4 手动筛选模式

- App顶部/聊天界面提供明显的模式切换入口（"AI聊天 ⇄ 手动筛选"），用户可随时切换，不强制使用聊天模式。
- **字段范围（回应第十二章开放问题3）**：本产品核心人群是休闲/自由行游客（见第四章），对精确的"距离地标XX米"类数字不敏感，更容易被AI用模糊语言（"离地铁近""闹市边上"）满足，因此手动模式字段在行业通行字段基础上做精简：保留目的地/位置、入住离店日期、价格区间、星级、房型；设施只保留早餐/停车/健身房三项（WiFi、空调在数据集里100%覆盖，勾不勾都不缩小结果范围，没有筛选价值；泳池已作为偏好标签的一个选项存在，设施区不重复出现同一概念）；不设"距离地标/市中心距离"筛选维度（该信息改为在结果卡片上以文字展示，供参考但不作为筛选条件）；排序方式：价格从低到高。商务差旅人群更依赖位置/距离筛选，留待路线图中的差旅场景再评估。
- **偏好标签**：在设施之外提供一组"偏好标签"多选筛选，覆盖"海景""亲子"这类软性偏好；标签集合直接取自酒店数据里的标签词表，而不是写死的固定列表——酒店数据增加新标签时，这里会自动多出对应选项。命中其中任意一个标签即视为匹配（不要求同时满足所有勾选标签，否则组合越多结果越容易清零）。这套标签词表与6.2节AI对话侧用来匹配偏好的关键词是同一套，保证两种模式对"偏好"的理解一致。标签分组展示，分组命名贴近OTA行业惯例——"位置""景观房型""适合人群"三组，每组5个同类高频标签，按命中酒店数从高到低排列：
  - 位置：近古城、近地铁、市中心、近火车站、近海滩。
  - 景观房型：大床房、海景房、湖景、庭院、山景。
  - 适合人群：亲子友好、情侣推荐、商务出行、朋友出游、独自旅行。
  - 三个分类内部都是严格同类的高频选项；不属于任何预设分组的新标签会归入"其他偏好"兜底分组，保证分组规则不会让新增标签消失不见。换成第八章的真实数据集后，"位置""景观房型"这两组目前只有"大床房"等个别标签能对上号（数据集没有精确位置/景观信息）；干净/位置好/服务好/设施好这几个由真实评分衍生出的标签目前都落进"其他偏好"兜底分组——这是数据集本身的覆盖范围问题，不是分组设计的问题。
- **状态保留**：切换AI聊天⇄手动筛选时，两侧各自"进行中"的状态——聊天记录、已选筛选条件——都必须保留，不能因为切换就清空重来。验收时应覆盖"聊了几句→切到筛选看结果→切回聊天→之前的对话还在"这条路径。
- 两种模式共享同一套酒店数据和评论数据，用户可以在筛选结果中随时唤起AI（"帮我看看这几家哪个更安静"），实现模式间的无缝衔接，而非完全割裂的两个功能。

### 6.5 预订与支付闭环

- 用户选定酒店房型后，在App内完成完整预订流程：确认信息 → 点击"确认预订并支付" → 订单确认，不跳转第三方平台，也不需要额外一步单独的付款页——点一次就直接完成模拟支付并创建订单（MVP阶段模拟支付，见第八章）。
- **预订成功页附带的满意度快问（AI推荐/手动筛选的订单都问）**：订单创建成功、显示"预订成功！"的同一屏，会紧接着出现一个满意度快问——`source='ai_chat'`的单问"这次AI帮你推荐的酒店，你满意吗？"，手动筛选的单问"这次预订的选择，你满意吗？"，都配"满意/一般/不满意/先跳过"四个快捷选项，选完之后也和6.3节的入住体验一样，提供可选的文字/照片/语音详细反馈+"是否同意展示在评论区"的选择；这是6.3节"满意度"追问的唯一发生时机——就在下单这一刻问，不是等入住之后（入住之后问的是另一件事，"入住体验"，见6.3）。
- **登录时机（回应第十二章开放问题4）**：浏览和聊天阶段全程无需登录。触发点为用户点击"确认预订并支付"按钮的瞬间——若未登录，插入登录/注册弹层（手机号+验证码，MVP阶段最短路径，不接入第三方账号体系），完成后自动返回预订确认页，且保留用户此前选定的房型、日期、人数等信息，不清空已进行的对话上下文或重新开始预订流程。
- **返回入口**：不管是从AI推荐卡片还是手动筛选结果点进这个预订详情弹层，弹层顶部（拖拽条下方，滚动内容区域之上，始终可见、不随内容滚动而消失）都有一个圆形返回箭头按钮，点击直接关闭整个弹层。
- **退出登录**：右上角头像在已登录状态下点击会浮现一个小的"退出登录"按钮（头像本身始终是干净的纯图标，不常驻文字），点这个按钮才真正退出，点别处会收起不生效；退出后清空当前登录状态和称呼（见6.1）、直接回到登录弹层，AI回到不带名字的通用问候语；如果之后用同一个手机号登录，会直接恢复该账号锁定的名字，不需要重新问一次。**退出的同时会开启一个全新的聊天会话**——聊天记录清空、AI之前从对话里提取的目的地/预算/偏好等上下文一并清空，不会把上一位登录者的对话内容带到下一次登录里；旧会话在服务端也会同步清理。

---

## 七、视觉与交互设计规范

本章记录产品的视觉语言和关键交互效果要求，作为UI设计和前端实现的基准。已根据本章规范产出一版UI Demo（见文末链接），可作为验收视觉方向时的参照物。

### 7.1 版式风格：对标小红书(RedNote)

整体版式不采用传统OTA的"表格式列表"（干瘪的价格+星级+一行文字），而是参考小红书的排版逻辑——图片优先、卡片化、瀑布流布局，让"酒店推荐"和"评论区"都具备强内容感和可信赖感：

- 酒店推荐结果采用双列瀑布流卡片（图片在上，标题/价格/标签在下），而非单列长列表。
- 每张卡片保留"真实评论摘录 + 小尺寸头像/来源标注"的元素组合，视觉上接近小红书笔记卡片的"图+文+作者信息"结构，强化"这是真实住客说的"的可信感。
- 评论区展示同样借鉴小红书笔记卡片的排版：头像+昵称+标签在前，正文简短，弱化传统评论区的"长文本+星级"呆板样式。

### 7.2 色板（Color Palette）

| 色名 | HEX | RGB | CMYK | 用途 |
|---|---|---|---|---|
| Merino（米白） | #F5EEDD | 245, 238, 221 | 0%, 3%, 10%, 4% | 页面背景底色，营造温暖、轻松的旅行氛围 |
| Rock Blue（浅蓝） | #84B3CE | 132, 179, 206 | 36%, 13%, 0%, 19% | 中间色：卡片辅助背景、标签底色、次要强调、AI头像底色 |
| Venice Blue（深蓝） | #16587B | 22, 88, 123 | 82%, 28%, 0%, 52% | 主强调色：标题文字、正文强调、按钮、AI消息气泡、选中态 |

三色之外不额外引入新色相；需要的浅/深变体通过对以上三色调整透明度或明度得到（例如卡片标签底色使用Rock Blue的低透明度版本），保持色彩系统的统一性。

### 7.3 字体

- **标题/品牌字**：略带复古、圆润感的衬线展示字体（Demo中使用Yeseva One），用于App名"出发喽"、酒店名称、屏幕标题，呼应用户提供的品牌调性参考图。
- **正文/UI字**：同样偏圆润、友好的无衬线字体（Demo中使用Quicksand），用于聊天文字、按钮、标签等界面文本，避免生硬的工具感。

### 7.4 AI角色形象（头像）

AI助手需要一个专属的视觉形象，而不是通用的机器人图标或纯色圆点：

- 设计一个简洁的拟人化/吉祥物风格头像（Demo中采用了一个笑脸小太阳形象，纯色板配色），在聊天气泡旁反复出现，建立"这是一个有温度的助手"的认知，而非冷冰冰的系统提示。
- 头像需要有轻微的动效（如呼吸/浮动动画），在AI"正在输入"或刚发出消息时给用户更强的"对方是活的、在跟你互动"的感觉，而不是静态图标。

### 7.5 关键交互效果

以下交互效果需在正式开发中实现，Demo阶段已做部分验证：

- **AI聊天 ⇄ 手动筛选切换**：点击切换时有平滑的背景滑块过渡动画，且内容区域随之切换，让用户明确感知当前所处模式（Demo中已实现可点击的模式切换）。
- **推荐卡片**：卡片支持点赞/收藏式的小交互（点击可切换选中状态），推荐理由摘要可点击查看更完整的评论依据。
- **筛选器**：星级、设施等标签点击后有明显的选中态反馈（填色+文字反色），价格区间为可拖拽的双滑块。
- **AI追问的快捷回复气泡**：点击即选中并触发AI的下一句回应，强调"轻量互动，不是填表"的体验，而不是提交后无反馈的静态表单。
- **输入框与按钮**：输入框聚焦态、按钮点击态需要有轻微的视觉反馈（边框高亮、按压缩放等），避免界面显得"死"。

### 7.6 UI Demo

已产出一版静态+局部可交互的UI Demo，涵盖AI聊天首页、评论驱动的酒店推荐（瀑布流卡片）、手动筛选模式、AI主动追问反馈闭环四个核心场景，配色、字体、AI头像形象均按本章规范执行，作为后续视觉稿和前端开发的基准参照。

> 视觉设计稿（4屏画布，局部可点）：[出发喽 App Demo](https://claude.ai/code/artifact/bf5b07e0-9b66-4c1d-bffb-dd8d7d6e4789)
> 完整可交互原型（真的能聊天、下单、看反馈闭环）：[出发喽](https://claude.ai/code/artifact/67fb48cd-b9a4-4d00-9c87-d13b5ab58eb6)

> 以上两份是评审用的静态/半交互Demo。除此之外，项目仓库里已经有一版可以真实运行的全栈实现（`backend/` Node+Express+SQLite，`frontend/` React+Vite），AI对话和推荐理由由真实LLM API驱动、数据落库持久化，不再是模拟数据流转，运行方式见仓库 `README.md`。

### 7.7 各屏幕设计详情（对应Demo中的真实效果）

以下是Demo中四屏的详细设计说明，均已在画布中做成**可点击的真实交互效果**（不是纯静态截图），供开发和评审对照。

**① AI聊天首页**

- 顶部品牌区：App名"出发喽"（Yeseva One展示字体，Venice Blue），右上角是账号入口——始终是一个干净的纯圆形头像图标，不常驻任何文字。未登录时点击打开登录弹层；已登录时点击会在头像下方浮现一个小的"退出登录"按钮，点这个按钮才真正退出，点别处收起不生效；退出后直接回到登录弹层，方便立刻用别的手机号重新登录。
- 模式切换胶囊："AI推荐 / 手动筛选"双段式切换器（标签文案用"AI推荐"而不是"AI聊天"，更准确地描述这个模式在做的事——给推荐，不只是聊天），选中态为Venice Blue底、白字。点击"手动筛选"时，**同一屏幕的内容区域会实时切换**为一个精简版筛选表单（目的地、日期、价格区间、星级/设施标签、"查看X家酒店"按钮），并保留"跟AI聊聊，让TA帮你从筛选结果里挑一挑"的返回入口——用于验证6.4节"两种模式无缝切换、不是两个割裂功能"的设计假设。
- 聊天区：AI消息气泡靠左，配专属AI吉祥物头像——一个笑脸小太阳造型（Venice Blue底色+白色光芒线条+白色圆点眼睛和笑脸），并带有2.2秒一次的呼吸缩放动画，让用户感觉"对方是活的、在跟你互动"，而不是静态系统图标。用户消息气泡靠右，Venice Blue底白字。AI"正在输入"状态显示文字"对方正在输入"+三个依次跳动的圆点，模仿主流聊天软件的打字提示效果。
- 气泡圆角采用不对称"对话尾巴"样式而非四角统一圆角：AI气泡为左下角小圆角（其余三角大圆角），用户气泡为右下角小圆角，视觉上分别指向头像/发送方，强化"这是谁在说话"的方向感。普通AI消息气泡都在同一条头像基准线上左对齐。
- 首次进入聊天且用户尚未输入时，展示2-3条示例快捷输入（真实覆盖城市：北京/澳门/杭州），点击即代替手动打字直接发送，降低"不知道该跟AI说什么"的冷启动门槛，验证6.1节"入口即对话"的可用性假设。这些示例固定展示在底部输入栏正上方的"快捷操作区"，不挂在聊天记录里。
- 底部输入栏：圆角输入框 + 圆形发送按钮（Venice Blue底、白色箭头图标）；其正上方是一个"快捷操作区"，有内容时才出现，展示示例快捷输入。

**② AI推荐酒店（真实评论驱动 + 小红书式瀑布流）**

- 沿用相同的顶部品牌区和模式切换胶囊。
- AI先发一条消息说明"帮你挑了几家真实住客评价不错的酒店"，随后是**双列瀑布流**（卡片高度错落，仿小红书首页信息流的排布方式，而非传统单列长列表）展示4家酒店卡片。
- 每张卡片结构：图片区（真实酒店优先展示核实过的真实照片，没有真实照片的酒店按标签匹配主题关键词展示占位图片，而非纯色块——如"海景房"配海滩/海景图、"湖景"配湖泊图，比纯色块更接近小红书"有图有真相"的观感；图片加载失败时优雅退回纯色块+图标，不能露出裂图；占位图基于酒店ID生成固定的"锁定"参数，保证同一家酒店任何时候展示的都是同一张图，不会每次刷新随机换一张不相关的图。右上角悬浮爱心图标，点击可切换点赞状态——点赞后图标变为实心白色，底部"XX人觉得有用"计数+1；未点赞为描边空心）→ 酒店名（Yeseva One）→ 价格 → 标签胶囊（如"海景房""安静"）→ 评论摘要小卡片（引号符号+一句基于真实评论生成的推荐理由，如"62%住客提到隔音好、能看日出"）→ 底部小头像图标+"来自真实住客评论"归因文字+点赞数。
- 这一屏是"真实评论驱动的可解释推荐"（6.2节）在界面上的直接呈现，也是本产品区别于携程/飞猪/同程的核心差异化视觉表达。

**③ 手动筛选模式**

- 模式切换胶囊显示"手动筛选"为选中态。
- 表单区：目的地输入行（默认"不限"，展示全部覆盖城市）、入住/离店日期双列展示、价格区间可拖拽双滑块（滑块下方带均匀分布的刻度线+价格标签，让区间不只是一行文字"¥300 - ¥1800"，而是能一眼看出大致落在整体范围的哪一段）、星级选择器（5颗可点亮的星形图标，模仿常见的星级评分交互——点击第N颗星代表"N星及以下"（例如点亮4颗＝只看3星和4星，排除5星），点亮的星带Venice Blue高光效果，未点亮为浅蓝描边；再点一次当前已点亮到的那颗星可清空回到"不限星级"，默认状态就是不限星级，不预选任何星星）、设施图标卡片（早餐/停车/健身房三项，点击可切换选中态；WiFi和空调因为在数据集里100%覆盖、筛选没有实际意义，不作为选项）。
- 底部"查看X家酒店"的结果数会随目的地、价格区间、星级、设施、偏好标签的勾选实时重新计算。
- 排序方式：不是独立卡片，也不是左右分居两端的两个元素，而是同一个按钮内部、作为一组一起水平居中的两段文字——主标题"查看X家酒店"和浅色小字"排序 价格从低到高"紧挨着显示在按钮正中间；整体只有一个可点击区域，点击任意位置效果相同，出现在偏好标签之后。
- 底部保留"随时可以喊AI帮你从筛选结果里挑一挑"的提示文字，呼应两种模式的衔接设计。

**④ AI主动追问反馈（数据飞轮闭环）**

- 这一屏对应6.3节里**入住日期已过去之后**才会触发的那次追问（"入住体验"）——AI推荐满意度已经在预订成功页面单独问过了，见6.5。
- 提问文字和四个快捷回复胶囊（很干净 / 一般 / 不太干净 / 先跳过）在**同一张卡片**里——提问句子本身就是"对了～你之前入住的『XX酒店』，住下来之后感觉干净吗？"这一整句，不是先来一条框架性的铺垫消息、选项再另起一块，卡片和聊天记录之间不隔开、不挪到别处。这张卡片的视觉样式和AI的普通聊天气泡完全统一——头像贴在气泡外侧、气泡左下角带尖角、最大宽度限制在78%，不是一个独立于聊天流之外、四角等圆的悬浮卡片。选中某个非跳过选项后，同一张卡片里往下展开一个可选的"详细反馈"（文字/照片/语音），提交后聊天记录里只保留结果回显：
  - 选择非"跳过"选项：提交后聊天记录里依次出现用户回复气泡（对应文案或用户自己写的详细文字）→ AI感谢气泡 → "评论区预览"卡片（模拟头像+"来自AI助手采集"标签+回答内容），完整展示6.3节"反哺评论区"的数据流转链路。
  - 选择"先跳过"：不生成评论区内容，且**只结束这一次对话里的追问**——订单不会被标记为"已问过"，下次用户打开一个新对话/新登录会话时还会再被问一次，直到真正给出一个实际答案为止；用于验证"允许用户拒绝/跳过追问"这一隐私相关的非功能需求（见第九章）。

**关于交互完成度的说明**：以上按钮点击、模式切换、点赞、筛选标签选中、快捷回复触发均为Demo中真实可点击的效果；价格区间滑块的实际拖拽交互已在"完整可交互原型"中实现（双滑块拖拽+结果数实时重算），仅"4屏静态画布"版本因画布设计工具本身的手势限制保留静态展示——这一差异不代表方案未验证，仅代表两份Demo的保真度不同。前端开发阶段真正需要补齐的是滑块背后的真实召回/计费逻辑（当前为简化规则模拟），而非拖拽交互本身。

---

## 八、数据与技术假设

由于国内主流OTA（携程、去哪儿、美团等）不对外开放真实酒店库存、价格及预订API，且课程项目周期内无法达成真实商业合作，本产品在MVP阶段采用以下数据假设，需在评审中明确说明：

| 数据类型 | 方案 | 说明 |
|---|---|---|
| 酒店静态信息（名称、城市、星级、设施、标签） | Kaggle上一份携程/Trip.com真实酒店评论数据集（4家真实酒店：澳门银河酒店、北京索菲特大酒店、纽约中城希尔顿、杭州洲至奢选华夏之心） | 数据真实，酒店主体、城市、评论文本均为真实内容；标签/设施**全部由真实字段算法推导，没有一条手写**——干净/位置好/服务好/设施好来自每条评论真实的分项评分（≥4.5阈值），商务出行/亲子友好/情侣推荐等来自评论真实的"出行类型"字段，大床房来自真实房型名称关键词，早餐/停车/健身房来自评论正文关键词出现次数（见`backend/src/db/seed.ts`） |
| 用户评论文本 | 同上数据集，662条真实中文评论 | 作为6.2、6.3节功能的底层语料；导入时只保留中文原文（过滤掉英文/其他语言评论），用于6.2的评论优先排序和"住客高频提到"统计 |
| 实时价格、库存/可订状态 | 规则/随机生成模拟 | 数据集本身不含价格/库存信息，仍按星级设定价格区间，叠加周末溢价、随机浮动等规则，不追求绝对真实，仅保证Demo内部逻辑自洽 |
| 用户历史订单（用于触发6.3追问功能） | 预设demo账号的模拟历史订单 | 因MVP阶段无真实交易积累，需人为构造若干条"已完成订单未评价"的测试数据 |
| 酒店图片 | 4家酒店中3家已换成核实过的真实照片（Wikimedia Commons，知识共享许可） | 逐家核实过对应真实建筑（如北京索菲特、纽约希尔顿门头"HILTON"字样、澳门银河度假村外观）；剩下1家（杭州洲至奢选，2020年开业的IHG精选品牌小众酒店）在Commons上没有可用照片，仍用按主题关键词生成的占位图兜底（见7.7②），`hotels.real_image_url`字段为空时自动回退 |

**技术方向假设**：AI推荐理由的生成建议采用检索增强生成（RAG）模式——先从评论语料中检索与用户偏好相关的片段，再基于检索结果生成摘要式推荐理由，而非让大模型完全自由生成，以降低幻觉风险并保证理由可追溯。

---

## 九、非功能需求与风险

- **内容真实性风险**：AI在聊天中采集到的"随口反馈"（如6.3节）信息量和严谨度低于正式评论，直接混入评论区展示可能引发真实性质疑或被恶意利用（刷好评/差评）。对策：明确标签区分来源，并预留人工/规则审核机制（本期作为开放问题记录，具体审核规则待后续设计）。6.3节可选的文字/照片/语音详细反馈同样未接入审核，风险面比纯结构化选项更大一些（自由文字/图片理论上可以包含不当内容），仍需要后续设计审核机制才能上生产。
- **AI理解准确度风险**：这是竞品（携程问道）被集中吐槽的短板，也是本产品体验的生命线。验收阶段需要专门设计测试用例，覆盖"多轮对话中途修改条件""同一会话内的上下文保持""模糊表达的追问"等场景。
- **响应速度（回应第十二章开放问题2）**：对话式交互对AI响应延迟更敏感（用户在"聊天"心理预期下对等待的容忍度低于填表单）。本阶段设定的体验基线（假设值，供评审调整）：用户发送后1.5秒内必须出现"正在输入"动效，避免用户怀疑卡死；简单追问/修改条件类回复应在3秒内给出文字回应；首次生成推荐（含评论检索+摘要生成）允许略长，但5秒仍是用户耐心的软上限——若预计超过5秒，应采用渐进式反馈（先返回一句"帮你翻翻真实评论"过渡语，再流式吐出推荐卡片），而不是让用户对着空白等待。
- **隐私**：AI主动询问用户历史入住体验，涉及调用用户订单历史，需要在产品设计上明确告知用户数据用途，并允许用户拒绝/跳过追问。
- **数据画像偏差**：目前覆盖的4家酒店（澳门银河、北京索菲特、纽约希尔顿、杭州洲至奢选）入住场景偏商务/高端，和第四章设定的"休闲游客看海放松"目标画像不完全匹配；这是真实数据集选择范围有限导致的，不代表产品定位改变，后续爬虫补充更多、更参差的休闲/度假类酒店数据后应会缓解。
- **偏好词匹配偏差**：AI从对话中提取的偏好关键词是自由文本（例如可能提取出"商务酒店"），不一定和评论数据里真实存在的标签词（如"商务出行"）精确匹配；6.2节的"评论优先排序"和"住客高频提到"两个功能各自基于精确关键词匹配和全量统计独立工作、不受此影响，但那一条没有精确匹配上的偏好词本身不会在推荐/筛选逻辑里生效。
- **指标样本量不足**：第十章"推荐采纳率"等指标现在有真实埋点，但目前只有4家酒店、订单量级还很小，测出来的百分比暂时只能验证"埋点和流程本身是否work"，还谈不上统计显著性；等第八章提到的爬虫补数据计划推进、真实订单量上来之后，这些数字才有参考价值。
- **语音反馈不进入检索/统计**：6.3节的语音详细反馈只存储音频、不做语音转文字，因此不会被6.2节的评论关键词检索或"住客高频提到"统计捕捉到——它目前更像一份"附件"，而不是能被搜索/统计的结构化语料；后续若要让语音反馈真正反哺数据飞轮，需要接入语音转文字服务。
- **推荐采纳率的指标有效性局限**：第十章"推荐采纳率"依赖的是模拟支付（PRD §8：点击"确认预订并支付"必定成功，零成本零摩擦）——这意味着它现在实际测量的是"用户愿意点确认按钮"，不是"用户愿意为这个推荐真实付费"，没有真实支付摩擦时这个数字会被系统性推高，不能直接当作"AI推荐值不值得让人掏钱"的证据。评估后决定暂不接入真实/沙箱支付网关（会显著增加工程量），继续如实标注；理解这个数字时应当把它当作"完成预订这个动作的转化率"而非"付费意愿"的代理指标。第十章"转化漏斗"里"打开详情但没点确认"这一段仍能看到真实流失，是这条局限之外一个独立、真实的信号。

---

## 十、成功指标（MVP阶段，以产品/体验指标为主）

由于是课程项目而非真实商业产品，本阶段成功指标以体验验证为主，暂不设置真实营收/留存类商业指标：

- **决策效率**：从打开App到完成预订的平均耗时/交互轮次，对比传统筛选模式是否有效缩短决策路径。
- **推荐采纳率**：AI首次给出的推荐结果中，用户最终选择其一完成预订的比例。
- **推荐理由的说服力**：用户对"AI推荐理由是否有说服力/是否基于真实评论感到可信"的主观满意度（可通过用户测试问卷获取）。
- **模式使用分布**：AI聊天模式与手动筛选模式的使用占比，验证"双模式并存"假设是否成立（即是否存在明显偏好某一模式的用户群体）。
- **上下文理解正确率**：测试用例中，AI正确保持多轮对话上下文（不丢失此前提到的城市/预算/偏好）的比例，作为对标携程问道短板的直接验证指标。
- **反馈采集转化率**：AI主动追问后，用户实际给出有效反馈的比例，验证"聊天式追评"是否比传统评价表单更有效。

**指标落地**：上面几项里，"推荐采纳率""决策效率（对话轮次这一部分）""推荐理由的说服力（改问'推荐满意度'）""反馈采集转化率"这四项已经从"待验证的目标"变成能实测的数字，具体机制：

| 指标 | 怎么测出来的 |
|---|---|
| 推荐采纳率 | `orders`表的`source`字段区分订单是从AI推荐卡片点进去下的（`ai_chat`）还是手动筛选下的（`manual_filter`），采纳率＝`ai_chat`订单数／全部订单数——⚠️受模拟支付影响，测的是"确认预订"转化率而非真实付费意愿，见第九章局限 |
| 决策效率（对话轮次） | `orders.chat_turns_before_order`记录下单前该会话已发送的用户消息数，服务端根据session记录直接写入，不能从前端伪造 |
| 推荐满意度 | 6.5节预订成功页上的满意度快问（下单成功那一刻就问，不用等入住）——现在AI推荐和手动筛选的订单都会问，但这条指标本身仍然只统计`source='ai_chat'`的那部分（后端查询按订单来源过滤），因为它衡量的是"AI推荐质量"，不是"整体预订满意度"，满意/一般/不满意/跳过的分布 |
| 反馈采集转化率 | 6.3节两问（推荐满意度、入住体验）各自的"答了 vs 跳过"比例 |
| 转化漏斗 | `booking_funnel_events`表记录"打开预订弹层→点击确认预订→完成支付"三个阶段的埋点，能看到"打开了但没点确认"这一段的真实流失人数；点击"确认预订并支付"之后走的是模拟支付，通常会立即成功变成订单，"点确认→完成支付"这一段能看到的流失主要来自点确认后取消登录弹层等边缘情况，不是真实的支付摩擦 |

内部调试页`/?debug=metrics`（未接入主导航）实时展示以上数字。"模式使用分布""上下文理解正确率"两项仍需专门的用户测试/测试用例覆盖，尚未有埋点。样本量方面的局限见第九章。

**如何手动测试"推荐满意度""入住体验"这两项**：从AI聊天推荐进入并完成支付，"推荐满意度"会**立刻**出现在预订成功页上，不需要额外操作；从手动筛选进入完成支付则不会看到这一问（因为不涉及AI推荐）。"入住体验"这一问触发条件不同——只在订单**入住日期已经过去**时才会问，预订时的入住/离店日期默认是未来日期，所以正常走一遍流程不会立刻看到。测试时可以在预订详情页把入住/离店日期手动改成过去的日期（日期输入框没有限制，可以随便选）再完成支付，然后刷新页面（追问检查在登录后每次页面挂载时跑一次）即可看到"入住体验"这一问；如果点了"先跳过"，这条订单不会被标记为已问过，下次刷新/重新登录时还会再被问一次。

---

## 十一、未来路线图（MVP之后）

- **长期个性化记忆**：跨会话记住用户偏好（如"该用户总是选安静的房间"），实现"越用越懂你"，对齐Airbnb、Expedia的行业方向。
- **覆盖商务差旅场景**：针对商务人群设计差异化的对话策略和信息采集逻辑。
- **真实酒店库存/支付对接**：探索与真实OTA或酒店集团的数据合作可能性。
- **反哺数据的质量治理机制**：为AI采集的用户反馈设计更完善的审核、可信度加权机制。
- **主动服务能力**：参考Expedia Romie的"AI礼宾"方向，探索行程变化、异常情况下的主动提醒和处理。

---

## 十二、开放问题（待后续决策）

以下开放问题已给出假设性结论（均为课程项目阶段的合理假设，非最终业务规则，评审中如有不同意见可重新开放讨论）：

| 开放问题 | 结论摘要 | 对应章节 |
|---|---|---|
| AI追问采集反馈的审核/质量控制规则 | 不引入人工审核/NLP审核，靠"结构化选项而非自由文本"+"单用户单酒店限一次"+"AI采集反馈占比设上限（假设30%）"三条规则兜底；可选的文字/照片/语音详细反馈同样暂未接入审核 | 6.3 |
| AI响应延迟的体验基线 | 1.5秒内出现"正在输入"、简单回复3秒内、首次推荐生成5秒为软上限，超时需渐进式反馈 | 九、非功能需求与风险 |
| 手动筛选字段是否精简 | 保留目的地/日期/价格/星级/房型，设施精简为早餐/停车/健身房三项，去掉"距离地标"筛选维度（仅作展示） | 6.4 |
| 登录/注册触发时机 | 全程浏览/聊天免登录，点击"确认预订并支付"时插入登录弹层，完成后回到预订页且不丢失已选信息 | 6.5 |
| 6.1节"轻量身份"（本地昵称）与6.5节手机号真实登录是否应合并成一套账号体系 | 已合并：称呼不再单独采集，第一次用某个手机号登录时登录框顺带问一次名字并锁定成账号名字，之后同一手机号登录都直接沿用、不重新问；退出登录会清空当前称呼和登录状态 | 6.1 / 6.5 |

后续评审或开发中发现的新问题可继续记录于此。

---

## 参考资料

- [携程"问道"实测评价——woshipm](https://www.woshipm.com/it/6125667.html)
- [AI旅行助手竞争格局分析（携程/同程/飞猪对比）——woshipm](https://www.woshipm.com/it/6202035.html)
- [飞猪"问一问/帮帮"功能介绍——163新闻](https://www.163.com/dy/article/L44K530J05129QAF.html)
- [同程旅行×DeepSeek AI旅行助手——新华网](http://www.news.cn/travel/20250310/f50a93eb972540bab5469976c619ecd6/c.html)
- [Expedia AI助手Romie介绍——HotelDive](https://www.hoteldive.com/news/expedia-ai-assistant-romie/716315/)
- [Airbnb 2026 AI搜索规划——TechCrunch](https://techcrunch.com/2026/02/13/airbnb-plans-to-bake-in-ai-features-for-search-discovery-and-support/)
