PORT=8787
OPENAI_API_KEY=sk-xxxxx
DB_PATH=./data/chufalou.sqlite

# Only needed when the frontend is deployed separately (e.g. on Vercel) —
# comma-separated list of allowed origins. Leave unset to allow all (fine for
# local dev and the merged single-service deployment).
# ALLOWED_ORIGIN=https://your-frontend.vercel.app
