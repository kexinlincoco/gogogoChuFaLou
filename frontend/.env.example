# Only needed when the frontend is deployed separately from the backend
# (e.g. frontend on Vercel, backend on Render). Leave unset for local dev
# (Vite proxy handles /api) and for the merged single-service deployment.
VITE_API_BASE_URL=https://your-backend.onrender.com
